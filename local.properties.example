# 이 파일을 local.properties 로 복사해서 채운다. local.properties 는 git에 올라가지 않는다.
# 경로는 역슬래시 이스케이프가 까다로우니 슬래시로 쓰는 게 편하다.
sdk.dir=C:/Users/<user>/AppData/Local/Android/Sdk
# 공공데이터포털 인증키(Decoding 키). 앱이 알아서 URL 인코딩한다.
DATA_GO_KR_KEY=여기에_인증키
